<div class="row">
  <div class="col-md-12 text-center">
    <%= bootstrap_form_for(@lead, layout: :inline) do |f| %>
      <%= f.text_field :name, hide_label: true, placeholder: "Name" %>
      <%= f.email_field :email, hide_label: true, placeholder: "Your Email" %>
      <%= f.primary "Submit" %>
    <% end %>
