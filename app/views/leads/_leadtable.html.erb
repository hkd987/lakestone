<table class="table table-striped">
  <tr>
    <th>Name</th>
    <th>E-Mail</th>
    <th>Edit</th>
  </tr>
<% @lead.each do |lead| %>
<tr>
  <td><%= lead.name %></td>
  <td><%= lead.email %></td>
  <td><%= link_to "Edit", edit_lead_path(@lead) %> </td>
</tr>
<% end %>
</tbody>
</table>






</table>
